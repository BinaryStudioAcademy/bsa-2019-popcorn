import React from 'react';
import { connect } from 'react-redux';

const Collesction: React.FC = () => {
	return <div className="Collections">Collections</div>;
};

export default connect()(Collesction);
