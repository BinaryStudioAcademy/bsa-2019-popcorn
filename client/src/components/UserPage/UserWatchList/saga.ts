import { all, call, put, takeEvery } from 'redux-saga/effects';

// export function* (action) {

// }

// function* watch...() {
// 	yield takeEvery(action type, saga function);
// }

export default function* review() {
	yield all([]);
}
